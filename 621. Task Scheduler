//621. Task Scheduler
按词频从大到小插入，新插入一个task时先填满前一个task的track空下的格子然后再回到前面开始等间隔往后排
按这种插入方法，按词频递减顺序插入，永远不会产生多余的idle space
最后有可能爆掉，这样就是tasks的长度啦
理解了好久0.0 不过学到东西了
//用range for 因为vector::size() 返回unsigned int  如果－1可能导致溢出；之前都没有注意这个问题以后要小心了！
